---
title: "EES-Data-Screener-Report"
author: "Cam Race"
date: "10 September 2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## START HERE

Run the following to load packages, load your file, and prepare for the screening
```{r}
# -------------------------------------
# IMPORTING FILES
# -------------------------------------

library(tidyverse)

metadata <- read_csv("data_metadata/output_2.meta.csv")
dataset <- read_csv("data_metadata/output_2.csv")

# -------------------------------------
# SETTING UP
# -------------------------------------

# Metadata slices
mfilters <- filter(metadata,col_type=="Filter")
#mfilter_groups <- filter group is not blank
mindicators <- filter(metadata,col_type=="Indicator")

# Datafile slices
filternames <- c(mfilters$col_name) 
indicatornames <- c(mindicators$col_name)

dfilters <- select(dataset, filternames)
dindicators <- select(dataset, indicatornames)
```

## Data file checks

